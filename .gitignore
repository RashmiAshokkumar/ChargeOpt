# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.venv/
venv/
.env
.env.*
!.env.example

# Django
*.sqlite3
/media/
/staticfiles/

# Node
node_modules/
.next/
out/
coverage/
*.tsbuildinfo

# OS/files
.DS_Store
Thumbs.db

# Project data
# NOTE: results.json, existing_stations.csv, and the cached raw OSM graphml ARE
# committed on purpose — the deployed backend (see backend/Procfile's release
# phase) reads/seeds from these frozen pipeline outputs rather than re-running
# the full osmnx/Overpass fetch + MILP solve on every deploy.
/data-pipeline/cache/
/frontend/.env.local
/backend/.env
